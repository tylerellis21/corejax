export interface ICorejaxApp {
    name: string;
    build(firstName: string, lastName: string): string;
}
export declare class Basic implements ICorejaxApp {
    name: string;
    build(firstName: string, lastName: string): string;
}
